# Processed Data Directory

Stores the refined datasets after the ETL and Validation Logic phases.

**Schema:**
* `user_variants_clean.csv`: Genotype data filtered for quality control.
* `clinvar_validated.csv`: List of variants cross-referenced against user genome.