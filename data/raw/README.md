# Raw Data Directory

This directory stores the immutable input data.

**Expected Files:**
* `variant_summary.txt.gz`: Raw dataset from ClinVar FTP (approx. 120MB).
* `pubmed_batch_*.json`: Intermediate responses from NCBI API.

*Note: Actual data is excluded from version control for privacy and size reasons.*